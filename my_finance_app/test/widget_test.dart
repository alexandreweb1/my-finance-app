import 'package:flutter_test/flutter_test.dart';

// Firebase-dependent app — integration tests require a running Firebase emulator.
// Unit and widget tests should be added per feature in their own test files.
void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
